{
  "name": "sharegrams",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test:uml-core": "npm run test --workspace=@sharegrams/uml-core",
    "test:api": "npm run test --workspace=@sharegrams/api",
    "test:web": "npm run test --workspace=@sharegrams/web",
    "test": "npm run test:uml-core && npm run test:api && npm run test:web",
    "build:uml-core": "npm run build --workspace=@sharegrams/uml-core",
    "dev:web": "npm run build:uml-core && npm run dev --workspace=@sharegrams/web",
    "dev:api": "npm run build:uml-core && npm run dev --workspace=@sharegrams/api",
    "prisma:migrate": "npm run prisma:migrate --workspace=@sharegrams/api"
  }
}
